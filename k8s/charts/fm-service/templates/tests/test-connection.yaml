apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fm-service.fullname" . }}-test-connection"
  labels:
    {{- include "fm-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fm-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
